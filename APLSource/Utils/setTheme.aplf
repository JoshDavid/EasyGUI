 setTheme←{
     ⍝ ⍵ ←→ Theme namespace
     ⍝ ⍺ ←← FormBuilder object
     isEmpty ⍵:0
     isNS←{9=⎕NC ⍵}
     theme←{w←'⍵' ⋄ isNS w:⍎⍵ ⋄ ⍵}⍵
     _←⍺.setPageClass theme.PageClass
     _←⍺.addStyle theme.CSS
     0
 }
