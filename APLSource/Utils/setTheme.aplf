 setTheme←{
     ⍝ ⍵ ←→ theme name
     ⍝ ⍺ ←← FormBuilder object
     theme←'' 'StackedForm' 'SimpleLayout' ⍝ Theme lists
     divClass←'' 'form-style-6' 'form-style-9' ⍝ Associated div class
     css←''##.CSS.StackedForm ##.CSS.SimpleLayout
     i←theme⍳⊂⍵
     _←⍺.setPageClass⊃i⌷divClass
     ⍺.addStyle i⌷css
 }
