 formCallback←{
 ⍝ This generic callback will close the form and update the user response passed in ⍺
     postdata←9⊃⍵
     v←parsePostData postdata
     userResponse.Query←{''≡∊⍵:¯1 ⋄ ⍵}v
     userResponse.Query←userResponse.ResultType{('Int'≡⍺):⍎⍕⍵ ⋄ ⍵}userResponse.Query
     _←⎕NQ obj'Close'
 }
