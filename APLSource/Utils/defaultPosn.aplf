 defaultPosn←{
⍝ ⍵ ←→ given size of the page
⍝ ← ←→ The posns you should use to have your page centered
⍝ This function assumes you are using 'Prop' as your 'Coord' value
⍝ If your size is over 100, it assumes you using Pixel and positions
⍝ in the upper left hand corner
     (⍵∨.>100):0 0
     50-⍵÷2
 }
