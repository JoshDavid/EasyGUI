 stdInt←{
 ⍝ Standard Intercept Protocol. Relies on V.17
     cbURL←'http://dyalog_root/cb' ⍝ URL of the callback
     tm←1 2⍴cbURL 1               ⍝ Trigger matrix
     'InterceptedURLs'tm           ⍝ Only intercept the callback
 }
