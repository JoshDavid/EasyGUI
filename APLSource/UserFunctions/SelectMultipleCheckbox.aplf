 SelectMultipleCheckbox←{
 ⍝ ⍵ ←→ List of options to select from
 ⍝ ← ←→ The indices of the User's choice
     ⎕PATH←'##.Utils'
     ⍺←''
     _←setupUserResponse'Int'
     h←⎕NEW HtmlFormBuilder 1
     _←h.addCheckboxInput ⍵
     size←25 15
     ⍺ renderPage size h
 }
