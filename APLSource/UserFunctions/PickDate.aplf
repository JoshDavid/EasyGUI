 PickDate←{
 ⍝ ⍵ ←→ Text label to give the user context
 ⍝ ← ←→ The selected Date in 'YYYY-MM-DD' form
     ⎕PATH←'##.Utils'
     ⍺←''
     _←setupUserResponse'Char'
     h←⎕NEW HtmlFormBuilder 1
     ⍝w←nestIfSingle ⍵
     _←h.addDateInput ⍵
     size←25 15
     ⍺ renderPage size h
 }
