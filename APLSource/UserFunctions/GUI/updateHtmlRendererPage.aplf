 updateHtmlRendererPage←{
 ⍝ Callback function when 'Capture HTML' button is clicked
     hr←f.hr
     obj←hr.Form
     fn←{⊃⍵.SelItems/⍵.Items}f.sf.fns.list
     params←getUserInputs f.sf.params.⎕NL-9
     parameterEmpty←0∨.=≢¨params
     p←##.specifyParams'Title' 'Error'
     parameterEmpty:p ##.NotifyError'Populate all parameters!'
     _←(obj.⍎fn)params
     hr.HTML←obj.getHTML
 }
