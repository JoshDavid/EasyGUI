 updateHtmlRendererPage←{
     hr←f.hr
     obj←hr.Form
     fn←{⊃⍵.SelItems/⍵.Items}f.sf.fns.list
     params←f.sf.params.input.Text
     params←' '##.##.Utils.split params
     _←(obj.⍎fn)params       ⍝ Perform operation. might need to ⍎⍕ if vector or something
     hr.HTML←obj.getHTML
 }
