 getUserInputs←{
 ⍝ ⍵ ←→ list of objects in param subform
 ⍝ ← ←→ user text inputs
 ⍝   ←→ shape of result is equal to number of inputs
     ⎕PATH←'##.##.Utils'
     inputs←('i'=⊃¨⍵)/⍵
     text←(f.sf.params⍎⍕inputs).Value
     ⍝ Treat as one line if matrix (Multi line edit)
     isMatrix←1<⍴⍴text
     isMatrix:' '~⍨∊text
     (1<|≡text):' 'splitIfChar¨text
     ' 'splitIfChar text
 }
